name: liquid_flutter_ai_shared
description: Pure-Dart tool allow-rule model, matcher, merge, and schema helpers.
version: 0.0.1
repository: https://github.com/emdgroup-liquid/liquid-flutter

environment:
  sdk: ^3.10.7

dependencies:
  json_schema: ^5.2.2
  json_schema_builder: ^0.1.5

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
