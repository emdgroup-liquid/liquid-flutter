/// The role of a child widget within [LdMultiPanelLayout].
enum LdPanelRole {
  /// This child is the main panel (side panel).
  panel,

  /// This child is the body (fills remaining space).
  body,
}
